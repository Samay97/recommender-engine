import { ImageResolverPipe } from './image-resolver.pipe';

describe('ImageResolverPipe', () => {
    it('create an instance', () => {
        const pipe = new ImageResolverPipe();
        expect(pipe).toBeTruthy();
    });
});
